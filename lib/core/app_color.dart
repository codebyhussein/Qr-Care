import 'package:flutter/material.dart';

class AppColors {
  static Color mainColor = const Color(0xff197B8D);
  static Color primaryColor = const Color(0xff004854);
  static Color backgroundColor = const Color(0xffEFF1F1);
  static const BoxShadow defaultBoxShadow = BoxShadow(
    color: Color.fromRGBO(0, 0, 0, 0.25),
    offset: Offset(0, 4),
    blurRadius: 4,
    spreadRadius: 0,
  );
}
